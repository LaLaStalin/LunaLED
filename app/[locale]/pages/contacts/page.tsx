import { HeroBanner } from "@/components";

interface pageProps {}

const Contacts: React.FC<pageProps> = () => {
  return (
    <div>
      <HeroBanner heroTitle="Contacts" />
    </div>
  );
};

export default Contacts;
